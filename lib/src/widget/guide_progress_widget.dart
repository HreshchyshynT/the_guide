import "package:flutter/material.dart";

class GuideProgressWidget extends StatelessWidget {
  const GuideProgressWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}
